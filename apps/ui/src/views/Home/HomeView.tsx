import { AppView } from "@/components";

const HomeView = () => {

  return (
    <AppView>
        <h1> Welcome to SBIR Gen!</h1>
        <p> Login to get Started.</p>
    </AppView>
  );
};

export default HomeView;
